import { db } from './firebase';

// Por ahora dejamos estas funciones vacías para que la web cargue. 
// Más adelante las conectaremos a tus tablas de Firebase.
export const Query = () => ({
  execute: async () => []
});

export const User = {};
export const GymRecord = {};
export const FinanceTransaction = {};
export const CalendarEvent = {};
